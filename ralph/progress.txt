# Ralph Progress Log
# Project: Code Intelligence Digest - Codebase Refactoring
# Branch: ralph/codebase-refactoring
# Started: 2026-01-24

## Codebase Patterns
- Existing typecheck errors in `.next/` directory (generated Next.js files) and `history/` and `scripts/` directories - these are pre-existing and unrelated to refactoring work
- The `src/lib/pipeline/` directory contains the ranking pipeline components
- Recency scoring uses exponential decay formula: `0.2 + 0.8 * 2^(-ageDays / halfLifeDays)`
- Database abstraction: Use `getDbClient()` from `src/lib/db/driver.ts` for cross-database compatibility (SQLite dev, PostgreSQL prod)
- When querying with driver abstraction, cast result rows through `unknown` for type safety: `result.rows[0] as unknown as MyType`
- For upserts, check `db.driver` property and use PostgreSQL's `ON CONFLICT ... DO UPDATE SET col = EXCLUDED.col` vs SQLite's `INSERT OR REPLACE`

---

## 2026-01-24 - US-029
- What was implemented: Added deprecation notices to `src/lib/sync/inoreader-sync.ts` since the file is actively used by `/api/admin/sync` route
- Files changed:
  - `src/lib/sync/inoreader-sync.ts` (modified - added @deprecated JSDoc tags and TODO deprecation plan)
- **Learnings for future iterations:**
  - The `inoreader-sync.ts` file is imported by `/api/admin/sync/route.ts` (syncAllCategories, syncCategory functions)
  - Cannot delete it because it's actively used - added deprecation notice instead
  - The module-level @deprecated tag explains why to use `daily-sync.ts` instead (state management, resume capability, better error handling)
  - Each exported function has individual @deprecated tags pointing to the daily-sync.ts alternatives
  - TODO deprecation plan includes: 1) Update API route, 2) Migrate functions, 3) Delete file after migration
  - When evaluating deprecated code, always check for imports first with grep before deciding to delete
---

## 2026-01-24 - US-026
- What was implemented: N/A - `rankCategoryWithoutRecency` function does not exist in this branch, so there's nothing to delete
- Files changed: None
- **Learnings for future iterations:**
  - Confirmed via `grep` that `rankCategoryWithoutRecency` doesn't exist anywhere in the codebase (only in PRD/documentation)
  - This is consistent with US-012 and US-025 findings: the function exists only in `production` branch, not `main`
  - The unified `rankCategory` function with `includeRecency` option (added in US-024) replaces the need for a separate function
  - When PRD asks to delete a function that doesn't exist, verify with grep/search, mark as N/A, and document
---

## 2026-01-24 - US-025
- What was implemented: N/A - `rankCategoryWithoutRecency` function does not exist in this branch, so there are no callers to update
- Files changed: None
- **Learnings for future iterations:**
  - The `rankCategoryWithoutRecency` function was identified in US-012 as existing only in the `production` branch, not `main`
  - Since this refactoring branch was created from `main`, the function doesn't exist
  - The `RankOptions` interface with `includeRecency` option was added in US-024 and is ready for any future callers
  - When PRD references callers that don't exist, mark as N/A and document the reasoning
---

## 2026-01-24 - US-023
- What was implemented: Updated `src/config/categories.ts` to import and use the shared domain terms from `domain-terms.ts`
- Files changed:
  - `src/config/categories.ts` (modified - removed 55 lines, added 6 lines)
- **Learnings for future iterations:**
  - The `buildTermWeightMap()` function from `domain-terms.ts` returns a `Map<string, number>`
  - Used `Object.fromEntries(buildTermWeightMap())` to convert Map to Record for type compatibility
  - This maintains backwards compatibility for any code that uses `DOMAIN_TERM_WEIGHTS` as a `Record<string, number>`
  - The original DOMAIN_TERM_WEIGHTS had 34 terms; the shared module has 97+ terms (superset)
  - This completes the domain terms consolidation - both bm25.ts and categories.ts now use domain-terms.ts
---

## 2026-01-24 - US-022
- What was implemented: Updated `src/lib/pipeline/bm25.ts` to import domain terms from the shared `domain-terms.ts` module
- Files changed:
  - `src/lib/pipeline/bm25.ts` (modified - removed 123 lines, added 3 lines)
- **Learnings for future iterations:**
  - The `getTermsByCategory()` function from `domain-terms.ts` returns exactly the same structure as the original local `DOMAIN_TERMS` object
  - Structure: `Record<DomainCategory, { weight: number; terms: string[] }>`
  - This enables direct replacement without modifying any of the downstream code that uses `DOMAIN_TERMS`
  - The `CATEGORY_QUERIES` object that references `DOMAIN_TERMS.category.terms` continues to work unchanged
  - Removed 120+ lines of duplicate term definitions, leaving only 3 lines: import + function call
---

## 2026-01-24 - US-021
- What was implemented: Created `src/config/domain-terms.ts` consolidating term configurations from `bm25.ts` and `categories.ts`
- Files changed:
  - `src/config/domain-terms.ts` (created - 279 lines)
- **Learnings for future iterations:**
  - Consolidated 97 domain terms from two sources: `bm25.ts` DOMAIN_TERMS (categorized object) and `categories.ts` DOMAIN_TERM_WEIGHTS (flat term→weight map)
  - Structure uses `{ term: string, weight: number, category: DomainCategory }[]` as specified in PRD
  - Added helper functions: `getTermsForCategory()`, `getTermWeight()`, `buildTermWeightMap()`, `getTermsByCategory()`
  - `getTermsByCategory()` returns the same structure as original DOMAIN_TERMS in bm25.ts for backwards compatibility
  - Handled plural forms (e.g., "agent"/"agents", "vector database"/"vector databases") by including both but deduplicating in `getTermsByCategory()`
  - Weight tiers: code_search (1.6), ir/context (1.5), agentic (1.4), enterprise (1.3), devtools/llm_code (1.2), sdlc (1.0)
  - Used `as const` on arrays for readonly type safety
---

## 2026-01-24 - US-020
- What was implemented: Updated `src/lib/sync/weekly-sync.ts` to use the shared sync state module from `sync-state.ts`
- Files changed:
  - `src/lib/sync/weekly-sync.ts` (modified - removed 110 lines, added 9 lines)
- **Learnings for future iterations:**
  - Same pattern as US-019 (daily-sync.ts) - both sync files now share common state management
  - Replaced `getDbClient` import with `loadSyncState, saveSyncState, clearSyncState` from `./sync-state`
  - Removed local `SyncStateRow` interface (identical to the one removed from daily-sync.ts)
  - Removed local `getSyncState()`, `saveSyncState()`, `clearSyncState()` functions (88 lines)
  - Updated property access from snake_case to camelCase to match shared SyncState interface
  - All state function calls now pass `SYNC_ID` ('weekly-sync') as first parameter
  - This completes the sync state consolidation - both daily-sync and weekly-sync now use sync-state.ts
---

## 2026-01-24 - US-019
- What was implemented: Updated `src/lib/sync/daily-sync.ts` to use the shared sync state module from `sync-state.ts`
- Files changed:
  - `src/lib/sync/daily-sync.ts` (modified - removed 80 lines, added 9 lines)
- **Learnings for future iterations:**
  - Replaced `getSqlite` import with `loadSyncState, saveSyncState, clearSyncState` from `./sync-state`
  - Removed local `SyncStateRow` interface (now defined in sync-state.ts)
  - Removed local `getSyncState()`, `saveSyncState()`, `clearSyncState()` functions
  - Updated property access from snake_case (`items_processed`, `calls_used`, `continuation_token`) to camelCase (`itemsProcessed`, `callsUsed`, `continuationToken`)
  - All state function calls now use `await` since shared functions are async
  - All state function calls now pass `SYNC_ID` as first parameter
  - This reduces code duplication and ensures consistent database driver abstraction
---

## 2026-01-24 - US-018
- What was implemented: Created `src/lib/sync/sync-state.ts` with shared sync state management functions
- Files changed:
  - `src/lib/sync/sync-state.ts` (created - 194 lines)
- **Learnings for future iterations:**
  - SyncState interface uses camelCase for TypeScript, SyncStateRow interface uses snake_case matching database columns
  - Created `rowToSyncState()` helper function to convert between database and TypeScript formats
  - SyncStateInput interface allows partial state updates (continuationToken and error are optional)
  - Both daily-sync.ts and weekly-sync.ts have identical state management logic - this module unifies it
  - The syncId parameter allows different sync types (daily-sync, weekly-sync) to maintain separate state
  - Uses database driver abstraction with PostgreSQL/SQLite compatible upsert syntax
---

## 2026-01-24 - US-017
- What was implemented: Added comprehensive unit tests for `computeBoostMultiplier` function in `__tests__/lib/pipeline/scoring-utils.test.ts`
- Files changed:
  - `__tests__/lib/pipeline/scoring-utils.test.ts` (modified - added 24 new tests)
- **Learnings for future iterations:**
  - Test file already existed for computeRecencyScore tests - extended it with new describe block for computeBoostMultiplier
  - Boost tiers tested: 5.0x (sourcegraph), 3.5x (product names), 3.0x (3+ core terms), 2.5x (agent+context), 2.0x (2 core terms), 1.5x (1 core term), 1.0x (baseline)
  - "coding agent" is a core term itself, so when paired with another core term like "code intelligence", it results in 2.0x (2 terms), not 2.5x (agent+context)
  - The agent+context combo (2.5x) only triggers when "agent" or "agentic" appears separately from core terms, combined with code context terms
  - Constants (PRODUCT_NAMES, CORE_TERMS) are also tested for expected values and length
---

## 2026-01-24 - US-016
- What was implemented: Updated `src/lib/pipeline/rank.ts` to use the shared `computeBoostMultiplier` function from `scoring-utils.ts`
- Files changed:
  - `src/lib/pipeline/rank.ts` (modified - replaced inline boost logic with import)
- **Learnings for future iterations:**
  - Replaced ~50 lines of inline boost logic with 8 lines using shared function
  - Removed local `coreTerms` array that duplicated CORE_TERMS constant
  - The `computeBoostMultiplier` function already lowercases content internally, so we don't need `.toLowerCase()` on contentToSearch
  - Simplified boost logging: one unified debug log instead of tier-specific messages
  - The boostResult.matchedTerms array provides which terms triggered the boost for transparency
---

## 2026-01-24 - US-015
- What was implemented: N/A - `compute-scores.ts` does not exist in codebase
- Files changed: None
- **Learnings for future iterations:**
  - Same situation as US-002: PRD references `compute-scores.ts` but this file doesn't exist
  - The boost logic is only in `rank.ts` (covered by US-016)
---

## 2026-01-24 - US-014
- What was implemented: Added `computeBoostMultiplier` function to `src/lib/pipeline/scoring-utils.ts`
- Files changed:
  - `src/lib/pipeline/scoring-utils.ts` (modified - added BoostResult interface and computeBoostMultiplier function)
- **Learnings for future iterations:**
  - The computeBoostMultiplier function encapsulates the boost logic from rank.ts lines 99-152
  - Boost tiers: 5.0x (sourcegraph), 3.5x (product names), 3.0x (3+ core terms), 2.5x (agent+code context), 2.0x (2 core terms), 1.5x (1 core term), 1.0x (baseline)
  - Function takes `category` parameter even though unused, for future extensibility with category-specific boosts
  - Returns BoostResult with both multiplier and matchedTerms for transparency/debugging
  - The `eslint-disable-next-line @typescript-eslint/no-unused-vars` comment is needed for the unused category parameter
  - Uses PRODUCT_NAMES and CORE_TERMS constants defined earlier in the file
---

## 2026-01-24 - US-013
- What was implemented: Added `PRODUCT_NAMES` and `CORE_TERMS` constants to `src/lib/pipeline/scoring-utils.ts`
- Files changed:
  - `src/lib/pipeline/scoring-utils.ts` (modified - added boost constants)
- **Learnings for future iterations:**
  - PRODUCT_NAMES contains 11 AI coding tools: augment code, claude code, cursor, copilot, cody, aider, codegen, windsurf, void, trae, roo
  - CORE_TERMS contains 13 domain-specific terms from the existing `coreTerms` array in rank.ts
  - Both arrays use `as const` for readonly type safety and better TypeScript inference
  - JSDoc with category groupings makes constants self-documenting
  - These constants will be used by computeBoostMultiplier function (US-014) and to replace inline arrays in rank.ts (US-016)
---

## 2026-01-24 - US-012
- What was implemented: N/A - `rankCategoryWithoutRecency` function does not exist in this branch
- Files changed: None
- **Learnings for future iterations:**
  - The function `rankCategoryWithoutRecency` exists in the `production` branch but not in `main` branch
  - This refactoring branch (`ralph/codebase-refactoring`) was created from `main`, so the function is not present
  - When PRD references a function that doesn't exist, mark as N/A and document the branch divergence
  - Check git history with `git show <branch>:path/to/file.ts | grep functionName` to verify function existence across branches
---

## 2026-01-24 - US-011
- What was implemented: Updated `src/lib/pipeline/rank.ts` to use the shared `filterLowQualityItem` function from filter-patterns.ts
- Files changed:
  - `src/lib/pipeline/rank.ts` (modified - added import and quality filtering step)
- **Learnings for future iterations:**
  - The rankCategory function did not have BAD_TITLE_PATTERNS or BAD_URL_PATTERNS defined locally - these patterns existed in decompose.ts and reviewNewsletter.ts
  - Added quality filtering step after time window filter, before BM25 indexing to avoid wasting computation
  - Filter uses `qualityItems` variable to distinguish from `recentItems` (time-filtered) items
  - Logs filtered items at debug level with reason, and summary count at info level
  - The import path from pipeline to config is `../../config/filter-patterns`
---

## 2026-01-24 - US-010
- What was implemented: Added `filterLowQualityItem` function to `src/config/filter-patterns.ts`
- Files changed:
  - `src/config/filter-patterns.ts` (modified - added FilterResult interface and filterLowQualityItem function)
- **Learnings for future iterations:**
  - The filterLowQualityItem function checks in order: title length, title patterns, URL patterns, URL domains
  - Returns early with a reason string for the first match, avoiding unnecessary checks
  - MIN_TITLE_LENGTH constant (10 chars) is defined locally in the module since it's filter-specific
  - The function handles null/undefined titles gracefully with optional chaining
  - FeedItem type is imported from model.ts via the `@/src/lib/model` path alias
---

## 2026-01-24 - US-009
- What was implemented: Created `src/config/filter-patterns.ts` with consolidated bad URL and title patterns
- Files changed:
  - `src/config/filter-patterns.ts` (created - consolidated patterns from decompose.ts and reviewNewsletter.ts)
- **Learnings for future iterations:**
  - PRD referenced patterns in `rank.ts` but they were actually in `decompose.ts` and `reviewNewsletter.ts`
  - Consolidated to 21 BAD_URL_PATTERNS, 11 BAD_TITLE_PATTERNS, and 6 BAD_URL_DOMAINS
  - Pattern categories: newsletter collections, meta/admin pages, social aggregators, tracking URLs, digest collection domains
  - Used `as const` for readonly arrays to maintain type safety
  - JSDoc with category headers makes the patterns self-documenting
---

## 2026-01-24 - US-008
- What was implemented: Updated `app/api/items/route.ts` to import `VALID_CATEGORIES` from `@/src/lib/model` instead of defining it locally
- Files changed:
  - `app/api/items/route.ts` (modified - replaced local VALID_CATEGORIES with import from model.ts)
- **Learnings for future iterations:**
  - API routes can use the same pattern as sync files: import `{ Category, VALID_CATEGORIES }` from model.ts
  - The import path in Next.js app directory uses `@/src/lib/model` alias
  - This eliminates another duplicate VALID_CATEGORIES definition, bringing all category validation to a single source of truth
---

## 2026-01-24 - US-007
- What was implemented: Updated both sync files to import `VALID_CATEGORIES` from `src/lib/model.ts` instead of defining it locally
- Files changed:
  - `src/lib/sync/daily-sync.ts` (modified - replaced local VALID_CATEGORIES with import from model.ts)
  - `src/lib/sync/weekly-sync.ts` (modified - replaced local VALID_CATEGORIES with import from model.ts)
- **Learnings for future iterations:**
  - When consolidating duplicate constants, import from the central location (model.ts) and remove local definitions
  - Both sync files had identical VALID_CATEGORIES arrays - now use the single source of truth in model.ts
  - The import uses `{ Category, VALID_CATEGORIES }` from model.ts to get both the type and the constant
---

## 2026-01-24 - US-006
- What was implemented: Added `VALID_CATEGORIES` constant to `src/lib/model.ts`
- Files changed:
  - `src/lib/model.ts` (modified - added VALID_CATEGORIES export)
- **Learnings for future iterations:**
  - The `Category` type in model.ts defines the 7 valid categories: newsletters, podcasts, tech_articles, ai_news, product_news, community, research
  - VALID_CATEGORIES is exported as `readonly Category[]` with `as const` for type safety
  - This constant can be used for validation and iteration over categories throughout the codebase
---

## 2026-01-24 - US-005
- What was implemented: Updated `src/lib/sync/weekly-sync.ts` to use database driver abstraction
- Files changed:
  - `src/lib/sync/weekly-sync.ts` (modified - replaced getSqlite with getDbClient)
- **Learnings for future iterations:**
  - Replace `getSqlite` from `../db/index` with `getDbClient` from `../db/driver`
  - Convert synchronous state functions (`getSyncState`, `saveSyncState`, `clearSyncState`) to async
  - The `DatabaseClient` interface provides `query()` and `run()` methods returning Promises
  - Query results use `result.rows` array, need to cast through `unknown` for type safety
  - For upserts, check `db.driver === 'postgres'` and use appropriate syntax
  - All callers of async state functions need `await`
---

## 2026-01-24 - US-004
- What was implemented: Created unit tests for `computeRecencyScore` function in `scoring-utils.ts`
- Files changed:
  - `__tests__/lib/pipeline/scoring-utils.test.ts` (created - 14 tests)
- **Learnings for future iterations:**
  - Tests are in `__tests__/` directory mirroring the source structure (e.g., `__tests__/lib/pipeline/` for `src/lib/pipeline/`)
  - Use vitest's `vi.useFakeTimers()` and `vi.setSystemTime()` to mock Date.now() for deterministic date-based tests
  - The formula `0.2 + 0.8 * 2^(-ageDays / halfLifeDays)` produces:
    - Score = 1.0 at age=0
    - Score ≈ 0.6 at age=halfLifeDays
    - Score approaches 0.2 floor for very old content
  - Test covers edge cases: fresh content, half-life, old content, different half-life values, bounds, and monotonic decay
---

## 2026-01-24 - US-002
- What was implemented: N/A - `compute-scores.ts` does not exist in codebase
- Files changed: None
- **Learnings for future iterations:**
  - The PRD referenced `compute-scores.ts` but this file doesn't exist. The recency function was only in `rank.ts`
  - When PRD references a file that doesn't exist, mark as N/A and document why
---

## 2026-01-24 - US-003
- What was implemented: Updated `src/lib/pipeline/rank.ts` to use shared `computeRecencyScore` from `scoring-utils.ts`
- Files changed:
  - `src/lib/pipeline/rank.ts` (modified - removed local function, added import)
- **Learnings for future iterations:**
  - rank.ts is the main ranking pipeline file combining BM25, LLM scores, recency, and boosts
  - The local function used a slightly different clamping approach (`Math.max(0.2, Math.min(1.0, decayedScore))`) vs the shared utils which uses the mathematically equivalent formula
  - When replacing duplicated functions with imports, ensure the behavior is functionally equivalent
---

## 2026-01-24 - US-001
- What was implemented: Created `src/lib/pipeline/scoring-utils.ts` with unified `computeRecencyScore` function
- Files changed:
  - `src/lib/pipeline/scoring-utils.ts` (created)
- **Learnings for future iterations:**
  - The original formula in rank.ts used `Math.max(0.2, Math.min(1.0, decayedScore))` as a clamp, but the PRD formula `0.2 + 0.8 * Math.pow(2, -ageDays / halfLifeDays)` is mathematically equivalent and cleaner
  - JSDoc with `@example` blocks is helpful for documenting scoring functions
  - Typecheck passes even though there are pre-existing errors in `.next/` generated files and some script files
---

## 2026-01-24 - US-024
- What was implemented: Added `RankOptions` interface and optional `options` parameter to `rankCategory` function in `rank.ts`
- Files changed:
  - `src/lib/pipeline/rank.ts` (modified - added interface, updated function signature, conditional logic)
- **Learnings for future iterations:**
  - The `RankOptions` interface provides `skipDateFilter` and `includeRecency` options
  - Default values: `skipDateFilter=false` (filter by date), `includeRecency=true` (include recency in scoring)
  - When `includeRecency=false`, recency score is set to `1.0` (neutral multiplier)
  - The `recentItems` variable declaration needed to change from `const` to `let` to support conditional assignment
  - Reasoning string now shows "Recency=disabled" when recency is turned off for better debugging
  - This enables callers to use `rankCategory(..., { includeRecency: false })` instead of needing a separate function
---

## 2026-01-24 - US-027
- What was implemented: Added BM25 score skip optimization to rank.ts
- Files changed:
  - `src/lib/db/items.ts` (added loadBM25ScoresForItems function - 42 lines)
  - `src/lib/pipeline/rank.ts` (modified - added caching logic)
- **Learnings for future iterations:**
  - The `item_scores` table already stores `bm25_score` per item/category, so we can leverage existing cached scores
  - The loadBM25ScoresForItems function queries by item_id AND category, returning most recent score for each item
  - The optimization separates items into two groups: those with cached scores (use stored value) and those needing computation (add to BM25 index)
  - When all items have cached scores, the BM25 index building is skipped entirely (saves CPU for large item sets)
  - Log message "Skipped BM25 for N items with cached scores" provides visibility into optimization effectiveness
  - The cached score lookup uses `cachedBM25Scores[item.id] ?? bm25Normalized.get(item.id) ?? 0` to prefer cached, then computed, then default to 0
---

## 2026-01-24 - US-028
- What was implemented: Added comprehensive unit tests for content filter patterns in `src/config/filter-patterns.ts`
- Files changed:
  - `__tests__/config/filter-patterns.test.ts` (created - 493 lines, 127 tests)
- **Learnings for future iterations:**
  - Test file location: `__tests__/config/` mirrors `src/config/` structure
  - FeedItem type from model.ts requires: id, streamId, sourceTitle, title, url, publishedAt, categories, category, raw
  - Pattern tests should be parametrized with `it.each()` for clear individual test cases
  - When testing filter order, choose test inputs carefully to avoid triggering earlier filters (e.g., use 13+ char titles to avoid length filter when testing pattern filter)
  - URL patterns may overlap with domain filters - test domain filtering with URLs that don't match patterns (e.g., bonobopress.com)
  - Pre-existing typecheck errors in `.next/`, `history/`, and `scripts/` directories should be ignored - they're unrelated to refactoring work
  - Some test failures like `promptRerank.test.ts` are pre-existing (import missing module)
---
